<%= render "shared/header" %>

<div class='main'>

<%# 写真検索 %>
  <h1>写真検索</h1>
  <%= search_form_for @p, url: photos_search_path do |f| %>
    <%= f.label :situation_eq, '撮影状況' %>
    <%# <%= f.search_field :situation.name %>
    <%= f.collection_select :situation_eq, @situations, :id, :name%>
    <br>
    <%= f.submit '検索' %>
    <br>
  <% end %>
  <%# 写真検索 %>

  <%# 検索結果 %>
  <h1>検索結果</h1>
  <% if @results.photo.present? %>
    <% @results.each do |result| %>
      <td>
      <br>
      <li>
        <%= result.title %>
      </li>
    <% end %>
  <% else %>
    該当する写真はありません
    <br>
    <%# 写真一覧 %>

  <% end %>

<%# 検索結果 %>

</div>


<%= render "shared/footer" %>

<!--ページの上部に戻る「↑」ボタン-->
<p class="nav-fix-pos-pagetop"><a href="#">↑</a></p>